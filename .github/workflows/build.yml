name: Android 16 Fast Tree Sync & Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Aggressive Freeing Up Disk Space
      run: |
        echo "=== المساحة قبل التنظيف ==="
        df -h
        # حذف حزم اللغات الكبيرة والمنصات المدمجة
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/powershell /share/swift /opt/pipx /usr/local/.ghcup /opt/az
        # تنظيف صور Docker ومخلفات حاويات النظام الخلفية التي تستهلك أكثر من 30 جيجابايت
        sudo docker image prune --all --force
        sudo docker builder prune --all --force
        # تنظيف عميق لمدير الحزم
        sudo apt-get clean
        sudo rm -rf /var/lib/apt/lists/*
        echo "=== المساحة بعد التنظيف العدواني ==="
        df -h

    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf lib32readline-dev lib32z1-dev liblz4-tool libncurses-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev openjdk-21-jdk python3 python-is-python3

    - name: Setup Repo Tool & Run Smart Build
      run: |
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        export PATH="$HOME/bin:$PATH"
        
        git config --global user.email "cloud-build@infinity.org"
        git config --global user.name "CloudBuilder"
        
        mkdir -p $GITHUB_WORKSPACE/android && cd $GITHUB_WORKSPACE/android
        
        # تهيئة بأقصى درجات الضغط والعمق 1 لتوفير المساحة
        repo init -u https://github.com/ProjectInfinity-X/manifest.git -b 16 --depth=1 --git-lfs
        
        # مزامنة ذكية بدون نسخ ثانوية لمنع امتلاء القرص
        repo sync -c -j$(nproc --all) --force-sync --clone-bundle=false --no-tags --current-branch
        
        # جلب شجرة ملفات السامسونج A04 الخاصة بك بدقة
        git clone https://github.com/DaniHasReal/android_device_samsung_a04.git -b lineage_22 device/samsung/a04 --depth=1
        git clone https://github.com/DaniHasReal/android_vendor_samsung_a04.git -b main vendor/samsung/a04 --depth=1
        git clone https://github.com/DaniTheSillyFem/kernel_samsung_a04.git -b linux-4.19.y kernel/samsung/a04 --depth=1
        
        # الإعداد وبدء التجميع والطبخ للروم
        source build/envsetup.sh
        lunch infinity_a04-userdebug
        mka bacon -j$(nproc --all)
